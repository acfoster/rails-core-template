#!/usr/bin/env bash
set -euo pipefail

. "$(dirname "$0")/bootstrap"

bundle exec rubocop
bundle exec rails tailwindcss:build
bundle exec rails zeitwerk:check
bundle exec rspec
bundle exec rails test
