<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= content_for?(:title) ? yield(:title) : "Document PDF" %></title>
    <style>
      body { font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif; font-size: 14px; color: #222; margin: 0; padding: 0; }
      .pdf-container { padding: 32px; }
      h1, h2, h3 { color: #333; margin-top: 1.5em; }
      table { width: 100%; border-collapse: collapse; margin: 1.5em 0; }
      th, td { border: 1px solid #ccc; padding: 8px; }
      th { background: #f5f5f5; }
      .section { margin-bottom: 2em; }
    </style>
    <%= yield :head %>
  </head>
  <body>
    <div class="pdf-container">
      <%= yield %>
    </div>
  </body>
</html>
